apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jira-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi  
  volumeName: jira-nfs-pv 
  storageClassName: ""
